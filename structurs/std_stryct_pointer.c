#include<stdio.h>
struct std{
	int id;
	char name[5];
	int marks;
	char grade;
};
void main()
{
	struct std *sptr, s[i];
	sptr=&s[i];
	printf("enter the id:");
	scanf("%d",&s[i]);
	printf("enter the name:");
	scanf("%s",&s[i]);
	printf("enter the marks:");
	scanf("%d",&s[i]);
	printf("enter the grade:");
	scanf(
